# Vercel KV / Upstash Redis (required for production, optional for local dev)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# AI Gateway (required for recipe ingestion)
AI_GATEWAY_API_KEY=

# Vercel Edge Config (required for feature flags)
EDGE_CONFIG=

# Flags SDK secret for Vercel Toolbar flags explorer
# Generate with: node -e "console.log(crypto.randomBytes(32).toString('base64url'))"
FLAGS_SECRET=
